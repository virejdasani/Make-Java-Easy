## getString();
```
// getString(String str);

        // This will prompt user for input and store the input in a variable called input.
        // It will then print the input.
        String str_input = makejavaeasy.getString("Enter string input: ");
        System.out.println(str_input);

6 lines of code reduced to 2
```

## getInt();

```
getString(String str); 

        // This will prompt user for input and store the input in a variable called input.
        // It will then print the input.
        Integer int_input = makejavaeasy.getInt("Enter int input: ");
        System.out.println(int_input);

 6 lines of code reduced to 2
```